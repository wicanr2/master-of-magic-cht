diff --git a/lib/font/cjk.go b/lib/font/cjk.go
new file mode 100644
index 0000000..f6924b6
--- /dev/null
+++ b/lib/font/cjk.go
@@ -0,0 +1,148 @@
+package font
+
+// CJK (繁體中文) glyph 支線 —— 工作魔法大帝中文化注入。
+//
+// 引擎原本只認 ASCII 32–127 (glyphIndex = int(c)-32);非 ASCII rune 在 doPrint /
+// PrintOutline / MeasureTextWidth 被靜默跳過。這裡提供「碼點 >= 0x2E80 改用 TTF rasterize
+// 的 glyph」這條支線,由 font.go 的繪字迴圈在 glyphIndex 超範圍時呼叫。
+//
+// 字級對齊 (Phase 2):CJK glyph 依「呼叫端字型的字高」rasterize,讓中英基線與行高一致、
+// 不破版。快取以 (rune, height) 為 key,face 以 height 為 key 各建一份。
+//
+// 字型來源:環境變數 MOM_CHT_FONT 指向 CJK TTF/TTC (預設找常見系統路徑)。
+// 找不到字型時所有函式回傳零值,呼叫端退回原本「跳過」行為,不影響英文。
+
+import (
+	"image"
+	"image/color"
+	"os"
+	"sync"
+
+	"github.com/hajimehoshi/ebiten/v2"
+	xfont "golang.org/x/image/font"
+	"golang.org/x/image/font/opentype"
+	"golang.org/x/image/font/sfnt"
+	"golang.org/x/image/math/fixed"
+)
+
+// CJK glyph 相對 cell 高度的基線位置 (0..1)。Noto 等字型 ink 約佔 em 上方,0.82 上下置中且不裁切。
+const cjkBaselineRatio = 0.82
+
+var cjkCandidatePaths = []string{
+	"/usr/share/fonts/opentype/noto/NotoSansCJK-Regular.ttc",
+	"/usr/share/fonts/truetype/noto/NotoSansCJK-Regular.ttc",
+	"/System/Library/Fonts/PingFang.ttc", // macOS
+	"C:\\Windows\\Fonts\\msjh.ttc",       // Windows 微軟正黑
+}
+
+type cjkKey struct {
+	r rune
+	h int
+}
+
+var (
+	cjkOnce  sync.Once
+	cjkFont  *sfnt.Font
+	cjkMu    sync.Mutex
+	cjkFaces = map[int]xfont.Face{}       // height -> face
+	cjkCache = map[cjkKey]*ebiten.Image{} // (rune,height) -> glyph image
+	cjkAdv   = map[cjkKey]int{}           // (rune,height) -> advance (px)
+)
+
+// CJK / 全形標點等範圍 (簡化版,Phase 2 再細分)。
+func isCJK(r rune) bool {
+	return r >= 0x2E80
+}
+
+func loadCJKFont() {
+	paths := cjkCandidatePaths
+	if env := os.Getenv("MOM_CHT_FONT"); env != "" {
+		paths = append([]string{env}, paths...)
+	}
+	var data []byte
+	for _, p := range paths {
+		if b, err := os.ReadFile(p); err == nil {
+			data = b
+			break
+		}
+	}
+	if data == nil {
+		return // 無字型:CJK 支線停用,退回原行為
+	}
+	tt, err := opentype.Parse(data)
+	if err != nil {
+		// .ttc (TrueType collection) 走 ParseCollection 取第 0 個 font
+		coll, cerr := opentype.ParseCollection(data)
+		if cerr != nil {
+			return
+		}
+		tt, err = coll.Font(0)
+		if err != nil {
+			return
+		}
+	}
+	cjkFont = tt
+}
+
+// cjkFaceFor 回傳對應字高的 face (lazy 建立並快取)。呼叫端須持有 cjkMu。
+func cjkFaceFor(height int) xfont.Face {
+	if f, ok := cjkFaces[height]; ok {
+		return f
+	}
+	f, err := opentype.NewFace(cjkFont, &opentype.FaceOptions{
+		Size: float64(height), DPI: 72, Hinting: xfont.HintingFull,
+	})
+	if err != nil {
+		return nil
+	}
+	cjkFaces[height] = f
+	return f
+}
+
+// cjkGlyphImage 回傳 rune 在指定字高 (px,通常為呼叫端字型的 GlyphHeight) 的預渲染 glyph
+// 與前進寬度 (px,未乘 scale)。CJK glyph 被框在 height 高的 cell 內以對齊行高。
+// 字型不可用或非 CJK 時回傳 (nil, 0),呼叫端應退回原 ASCII 處理。
+func cjkGlyphImage(r rune, height int) (*ebiten.Image, int) {
+	cjkOnce.Do(loadCJKFont)
+	if cjkFont == nil || !isCJK(r) || height <= 0 {
+		return nil, 0
+	}
+
+	cjkMu.Lock()
+	defer cjkMu.Unlock()
+
+	key := cjkKey{r, height}
+	if img, ok := cjkCache[key]; ok {
+		return img, cjkAdv[key]
+	}
+
+	face := cjkFaceFor(height)
+	if face == nil {
+		return nil, 0
+	}
+
+	advance, ok := face.GlyphAdvance(r)
+	if !ok {
+		return nil, 0
+	}
+	w := advance.Round()
+	if w <= 0 {
+		w = height
+	}
+
+	// cell 高度框在呼叫端字高,避免 CJG 行高溢出造成上下行重疊
+	rgba := image.NewRGBA(image.Rect(0, 0, w, height))
+	baseline := int(float64(height) * cjkBaselineRatio)
+	d := &xfont.Drawer{
+		Dst:  rgba,
+		Src:  image.NewUniform(color.White),
+		Face: face,
+		Dot:  fixed.P(0, baseline),
+	}
+	d.DrawString(string(r))
+
+	img := ebiten.NewImageFromImage(rgba)
+	cjkCache[key] = img
+	cjkAdv[key] = w
+	return img, w
+}
diff --git a/lib/font/font.go b/lib/font/font.go
index 7e84629..6fd0484 100644
--- a/lib/font/font.go
+++ b/lib/font/font.go
@@ -157,6 +157,8 @@ func (font *Font) PrintOutline(destination *ebiten.Image, edgeShader *ebiten.Sha
     shaderOptions.Uniforms["Time"] = float32(0)
     shaderOptions.ColorScale = colorScale
 
+    text = translateForDisplay(text)
+
     for _, c := range text {
         if c == '\n' {
             y += float64(font.GlyphHeight + font.internalFont.VerticalSpacing)
@@ -166,6 +168,20 @@ func (font *Font) PrintOutline(destination *ebiten.Image, edgeShader *ebiten.Sha
 
         glyphIndex := int(c) - 32
         if glyphIndex >= len(font.Glyphs) || glyphIndex < 0 {
+            // CJK 支線:outline 路徑同樣需要中文 glyph (否則中文在 PrintOutline 被丟棄)
+            if cjkImage, cjkAdv := cjkGlyphImage(c, font.GlyphHeight); cjkImage != nil {
+                var options ebiten.DrawImageOptions
+                options.GeoM.Scale(scale, scale)
+                options.GeoM.Translate(useX, y)
+                options.ColorScale = colorScale
+                destination.DrawImage(cjkImage, &options)
+
+                shaderOptions.GeoM = options.GeoM
+                shaderOptions.Images[0] = cjkImage
+                destination.DrawRectShader(cjkImage.Bounds().Dx(), cjkImage.Bounds().Dy(), edgeShader, &shaderOptions)
+
+                useX += float64(cjkAdv + font.internalFont.HorizontalSpacing) * scale
+            }
             continue
         }
 
@@ -196,6 +212,8 @@ func (font *Font) doPrint(destination *ebiten.Image, x float64, y float64, scale
     // TODO: this shadow distance could be a parameter
     distance := 3.0/4
 
+    text = translateForDisplay(text)
+
     for _, c := range text {
         if c == '\n' {
             y += float64(font.GlyphHeight + font.internalFont.VerticalSpacing)
@@ -205,6 +223,22 @@ func (font *Font) doPrint(destination *ebiten.Image, x float64, y float64, scale
 
         glyphIndex := int(c) - 32
         if glyphIndex >= len(font.Glyphs) || glyphIndex < 0 {
+            // CJK 支線:碼點超出 ASCII glyph 範圍時,改用 TTF rasterize 的中文 glyph
+            if cjkImage, cjkAdv := cjkGlyphImage(c, font.GlyphHeight); cjkImage != nil {
+                var options ebiten.DrawImageOptions
+                options.GeoM.Scale(scale, scale)
+                options.GeoM.Translate(useX, y)
+                if dropShadow {
+                    options.GeoM.Translate(scale*distance, scale*distance)
+                    options.ColorScale = colorScale
+                    options.ColorScale.ScaleWithColor(shadowColor)
+                    destination.DrawImage(cjkImage, &options)
+                    options.GeoM.Translate(-scale*distance, -scale*distance)
+                }
+                options.ColorScale = colorScale
+                destination.DrawImage(cjkImage, &options)
+                useX += float64(cjkAdv + font.internalFont.HorizontalSpacing) * scale
+            }
             continue
         }
 
@@ -247,6 +281,7 @@ func (font *Font) Print(image *ebiten.Image, x float64, y float64, scale float64
 
 func (font *Font) MeasureTextWidth(text string, scale float64) float64 {
     width := 0
+    text = translateForDisplay(text)
     for _, c := range text {
         if c == '\n' {
             continue
@@ -254,6 +289,10 @@ func (font *Font) MeasureTextWidth(text string, scale float64) float64 {
 
         glyphIndex := int(c) - 32
         if glyphIndex >= len(font.Glyphs) || glyphIndex < 0 {
+            // CJK 支線:中文 glyph 的寬度量測,讓置中/置右/換行計算正確
+            if _, cjkAdv := cjkGlyphImage(c, font.GlyphHeight); cjkAdv > 0 {
+                width += cjkAdv + font.internalFont.HorizontalSpacing
+            }
             continue
         }
 
@@ -344,23 +383,47 @@ func (font *Font) PrintOptions(image *ebiten.Image, x float64, y float64, option
     }
 }
 
-/* split the input text ABCD into two substrings AB and CD such that the pixel width of AB is less than maxWidth */
+/* split the input text ABCD into two substrings AB and CD such that the pixel width of AB is less than maxWidth.
+   CJK 感知:英文在空白處斷行;中文 (無空白) 可在任意字之間逐字斷行,避免整段中文無法換行被丟棄。 */
 func (font *Font) splitText(text string, maxWidth float64, scale float64) (string, string) {
-    size := font.MeasureTextWidth(text, scale)
-    if size < maxWidth {
+    if font.MeasureTextWidth(text, scale) < maxWidth {
         return text, ""
     }
 
-    parts := strings.Split(text, " ")
-    // FIXME: use binary search for this
-    for i := len(parts) - 1; i >= 0; i-- {
-        sofar := strings.Join(parts[0:i], " ")
-        if font.MeasureTextWidth(sofar, scale) < maxWidth {
-            return sofar, strings.Join(parts[i:len(parts)], " ")
+    runes := []rune(text)
+    lastBreak := -1 // rest 由此 index 起算的最後一個可斷點
+    breakAfterSpace := false
+
+    for i := 1; i <= len(runes); i++ {
+        if font.MeasureTextWidth(string(runes[:i]), scale) >= maxWidth {
+            if lastBreak > 0 {
+                if breakAfterSpace {
+                    return strings.TrimRight(string(runes[:lastBreak]), " "), string(runes[lastBreak:])
+                }
+                return string(runes[:lastBreak]), string(runes[lastBreak:])
+            }
+            // 無可斷點 (單一過長的英文字):至少切一個 rune,保證有進展、不回傳空字串
+            cut := i - 1
+            if cut < 1 {
+                cut = 1
+            }
+            return string(runes[:cut]), string(runes[cut:])
+        }
+        // 記錄可斷點 (rest 將從 runes[i] 起):空白之後,或中日韓字邊界
+        if i < len(runes) {
+            prev := runes[i-1]
+            next := runes[i]
+            if prev == ' ' {
+                lastBreak = i
+                breakAfterSpace = true
+            } else if isCJK(prev) || isCJK(next) {
+                lastBreak = i
+                breakAfterSpace = false
+            }
         }
     }
 
-    return "", text
+    return text, ""
 }
 
 func (font *Font) PrintWrap(image *ebiten.Image, x float64, y float64, maxWidth float64, options FontOptions, text string) {
@@ -405,6 +468,9 @@ func (font *Font) CreateWrappedText(maxWidth float64, scale float64, text string
     var lines []string
     var yPos float64 = 0
 
+    // 散文整段先翻譯,再斷行 (逐段翻譯無法命中整句譯文;中文需整段譯好再 CJK 斷行)
+    text = translateForDisplay(text)
+
     text = string(bytes.ReplaceAll([]byte(text), []byte{'\n'}, []byte{NewLine}))
 
     textLines := bytes.Split([]byte(text), []byte{NewLine})
diff --git a/lib/font/override.go b/lib/font/override.go
new file mode 100644
index 0000000..02416a6
--- /dev/null
+++ b/lib/font/override.go
@@ -0,0 +1,89 @@
+package font
+
+// 字串覆蓋層 (顯示層翻譯) —— 工作魔法大帝中文化。
+//
+// 設計重點 (見 ADR 0003):翻譯**只在顯示層**做,不碰引擎邏輯字串。
+// 因為引擎把英文名稱當邏輯 key 比對 (例 artifact/create-artifact.go:198
+// `if string(name) == "+6 Defense"`),若在資料讀取層改成中文會破壞遊戲邏輯。
+// 因此在 lib/font 的繪字進入點 (doPrint / PrintOutline / MeasureTextWidth) 把
+// 顯示字串換成中文,引擎內部仍用英文。
+//
+// 比對採 **trim 後精確比對**:引擎讀 LBX 名稱時會 bytes.Trim 去前後空白
+// (create-artifact.go:149),故表的 key 與輸入都先 TrimSpace 再查。
+//
+// 翻譯表來源:環境變數 MOM_CHT_STRINGS 指向一個目錄,載入其中所有 *.tsv
+// (欄位 source<TAB>zh<TAB>note,# 開頭與 header 略過)。未設定或查無對應時回傳原字串,
+// 不影響英文顯示。
+
+import (
+	"bufio"
+	"os"
+	"path/filepath"
+	"strings"
+	"sync"
+)
+
+var (
+	overrideOnce  sync.Once
+	overrideTable = map[string]string{}
+)
+
+func loadOverrideTable() {
+	dir := os.Getenv("MOM_CHT_STRINGS")
+	if dir == "" {
+		return
+	}
+	entries, err := os.ReadDir(dir)
+	if err != nil {
+		return
+	}
+	for _, e := range entries {
+		if e.IsDir() || !strings.HasSuffix(strings.ToLower(e.Name()), ".tsv") {
+			continue
+		}
+		loadOverrideFile(filepath.Join(dir, e.Name()))
+	}
+}
+
+func loadOverrideFile(path string) {
+	f, err := os.Open(path)
+	if err != nil {
+		return
+	}
+	defer f.Close()
+
+	scanner := bufio.NewScanner(f)
+	scanner.Buffer(make([]byte, 0, 64*1024), 1024*1024)
+	for scanner.Scan() {
+		line := scanner.Text()
+		if line == "" || strings.HasPrefix(strings.TrimSpace(line), "#") {
+			continue
+		}
+		cols := strings.Split(line, "\t")
+		if len(cols) < 2 {
+			continue
+		}
+		src := strings.TrimSpace(cols[0])
+		zh := strings.TrimSpace(cols[1])
+		if src == "" || src == "source" || zh == "" {
+			continue // 略過 header 與空譯文
+		}
+		// 先到先得:不覆寫已存在的 key (讓較早載入的表優先;同義變體可並存)
+		if _, exists := overrideTable[src]; !exists {
+			overrideTable[src] = zh
+		}
+	}
+}
+
+// translateForDisplay 把要顯示的字串換成中文 (trim 後精確比對)。
+// 表為空或查無對應時回傳原字串。引擎邏輯字串不受影響 (本函式只在繪字時呼叫)。
+func translateForDisplay(text string) string {
+	overrideOnce.Do(loadOverrideTable)
+	if len(overrideTable) == 0 {
+		return text
+	}
+	if zh, ok := overrideTable[strings.TrimSpace(text)]; ok {
+		return zh
+	}
+	return text
+}
diff --git a/test/cjk-render/main.go b/test/cjk-render/main.go
new file mode 100644
index 0000000..2d7f7ec
--- /dev/null
+++ b/test/cjk-render/main.go
@@ -0,0 +1,100 @@
+package main
+
+// Phase 1 真實引擎 CJK 驗證:用引擎真正的字型管線 (VaultFonts + lib/font 的
+// PrintDropShadow / Print / PrintOptions / PrintOutline) 畫中英混排,
+// 觸發 lib/font/cjk.go 的 CJK glyph 注入,觀察:
+//   1. 中文是否在真實引擎字型路徑渲染
+//   2. drop shadow / outline 是否套用到 CJK glyph
+//   3. 在 320×200 邏輯畫布、scale 後的版面是否破版 (overflow)
+//
+// 基於 test/font/main.go 改寫,僅替換繪製字串。按 Esc 結束。
+
+import (
+	"image/color"
+	"log"
+
+	"github.com/kazzmir/master-of-magic/game/magic/data"
+	"github.com/kazzmir/master-of-magic/game/magic/fonts"
+	"github.com/kazzmir/master-of-magic/game/magic/scale"
+	"github.com/kazzmir/master-of-magic/game/magic/util"
+	"github.com/kazzmir/master-of-magic/lib/font"
+	"github.com/kazzmir/master-of-magic/lib/lbx"
+
+	"github.com/hajimehoshi/ebiten/v2"
+	"github.com/hajimehoshi/ebiten/v2/inpututil"
+)
+
+type Engine struct {
+	LbxCache   *lbx.LbxCache
+	ImageCache util.ImageCache
+	VaultFonts *fonts.VaultFonts
+}
+
+func NewEngine() (*Engine, error) {
+	cache := lbx.AutoCache()
+	return &Engine{
+		LbxCache:   cache,
+		ImageCache: util.MakeImageCache(cache),
+		VaultFonts: fonts.MakeVaultFonts(cache),
+	}, nil
+}
+
+func (engine *Engine) Update() error {
+	for _, key := range inpututil.AppendJustPressedKeys(nil) {
+		if key == ebiten.KeyEscape || key == ebiten.KeyCapsLock {
+			return ebiten.Termination
+		}
+	}
+	return nil
+}
+
+func (engine *Engine) Draw(screen *ebiten.Image) {
+	screen.Fill(color.RGBA{R: 24, G: 18, B: 48, A: 255})
+
+	// 全部 scale 1,直接用螢幕座標排版 (PrintOptions 會把 y 乘 scale,混用會錯位)。
+	// item power 名稱都餵入「引擎實際使用的英文字串」,由 lib/font 顯示層覆蓋層
+	// (MOM_CHT_STRINGS 載 item-powers.tsv) 自動轉中文;引擎邏輯字串不動。
+
+	cs := ebiten.ColorScale{}
+	IN := engine.VaultFonts.ItemName
+	PW := engine.VaultFonts.PowerFont
+
+	// 標題 (literal CJK)
+	IN.PrintDropShadow(screen, 6, 6, 1, cs, "工作魔法大帝")
+	// 神器名:artifacts.tsv 尚未翻 → 保持英文 (證明覆蓋是選擇性的)
+	IN.Print(screen, 6, 26, 1, cs, "Sword of Mallana")
+	// 餵英文 → 覆蓋層自動轉中文
+	IN.Print(screen, 6, 42, 1, cs, "Holy Avenger")
+
+	// 物品能力:每條餵真實英文 power 名,自動翻譯
+	powers := []string{"Flaming", "Vampiric", "+3 Attack", "Magic Immunity", "Invisibility"}
+	py := 64.0
+	for _, p := range powers {
+		PW.Print(screen, 6, py, 1, cs, p)
+		py += 14
+	}
+
+	// 散文 CJK 斷行示範:長中文描述自動逐字換行 (maxWidth 內);
+	// 真實流程中這段由覆蓋層從英文整段翻好,再進 CreateWrappedText 斷行。
+	IN.Print(screen, 6, 150, 1, cs, "── 描述 (CJK 逐字斷行) ──")
+	desc := "烈焰之劍:每次攻擊都附帶火焰傷害,並使敵人陷入恐懼。持有者在戰場上所向披靡,無人能擋其鋒芒。"
+	engine.VaultFonts.ItemName.PrintWrap(screen, 6, 166, 240, font.FontOptions{Scale: 1}, desc)
+}
+
+func (engine *Engine) Layout(outsideWidth, outsideHeight int) (int, int) {
+	return scale.Scale2(data.ScreenWidth, data.ScreenHeight)
+}
+
+func main() {
+	log.SetFlags(log.Ldate | log.Lshortfile | log.Lmicroseconds)
+	ebiten.SetWindowSize(data.ScreenWidth*4, data.ScreenHeight*4)
+	ebiten.SetWindowTitle("cjk-render")
+	engine, err := NewEngine()
+	if err != nil {
+		log.Printf("engine load error: %v", err)
+		return
+	}
+	if err := ebiten.RunGame(engine); err != nil && err != ebiten.Termination {
+		log.Printf("run error: %v", err)
+	}
+}
